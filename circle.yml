test:
  override:
    - sbt clean coverage test:test coverageReport
  post:
    - bash <(curl -s https://codecov.io/bash)

deployment:
  production:
    branch: master
    commands:
      - sbt "+ publish"
